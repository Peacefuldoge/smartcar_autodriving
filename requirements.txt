numpy
opencv-python
Pillow

# Hardware-specific runtime dependencies are intentionally not pinned here:
# - v4l2capture
# - Paddle Lite Python API (`paddlelite`)
# - Paddle Mobile Python API (`paddlemobile`)
# Install the versions supplied for your embedded board / accelerator SDK.
